# dmemcg-permissions.conf
# Only writes the root cgroup subtree_control — safe at boot since root cgroup always exists.
# Per-user slice ownership is handled dynamically by dmemcg-setup.service.

# Enable dmem system-wide at root cgroup level
w /sys/fs/cgroup/cgroup.subtree_control - - - - +dmem
